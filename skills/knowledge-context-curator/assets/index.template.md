# Context Index

The catalog of all pages and agent-managed packs in this context repository. Each entry is a double-bracket link and a one-line summary. Agents read this first to identify candidate pages and recurring task context.

Keep summaries tight. The index is engineered to be cheap to read; a bloated index defeats its purpose.

When this file exceeds about 300 lines or the repository passes about 150 pages, shard into `{{CONTEXT_ROOT}}/indexes/<type>.md` and replace this file with a directory of shards. See `references/scaling-playbook.md` in the `knowledge-context-curator` skill for the migration procedure.

---

## Sources

(populated as sources are ingested)

## Entities

(populated as entity pages are created)

## Concepts

(populated as concept pages are created)

## Synthesis

(populated as durable analyses and query answers are filed back)

## Context Packs

(populated as agent-managed task packs are created)